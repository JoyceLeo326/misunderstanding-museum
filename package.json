{
  "name": "misunderstanding-museum",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check script.js && node --check mission.js && node --check sw.js",
    "security:secrets": "node scripts/check-secrets.mjs"
  }
}
